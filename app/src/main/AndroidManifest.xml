<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Уведомления (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Health Connect (заготовка для будущей интеграции) -->
<!--    <uses-permission android:name="android.permission.health.READ_BLOOD_GLUCOSE" />-->
<!--    <uses-permission android:name="android.permission.health.WRITE_BLOOD_GLUCOSE" />-->
<!--    <uses-permission android:name="android.permission.health.READ_INSULIN_DELIVERY" tools:ignore="ManifestOrder" />-->
<!--    <uses-permission android:name="android.permission.health.WRITE_INSULIN_DELIVERY" tools:ignore="ManifestOrder" />-->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DiaHelp">

        <activity
            android:name=".view.activity.MainActivity"
            android:exported="true">

            <!-- Health Connect: разрешает приложению быть выбранным в "Permissions rationale" -->
<!--            <intent-filter>-->
<!--                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />-->
<!--            </intent-filter>-->
        </activity>

        <activity android:name=".view.activity.RegistrationActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".view.activity.AuthorizationActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- FileProvider для экспорта CSV/PDF отчётов -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Health Connect: страница с описанием использования прав (заготовка) -->
<!--        <activity-alias-->
<!--            android:name=".PermissionsRationaleActivity"-->
<!--            android:exported="true"-->
<!--            android:targetActivity=".view.activity.MainActivity">-->
<!--            <intent-filter>-->
<!--                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />-->
<!--            </intent-filter>-->
<!--        </activity-alias>-->

    </application>

</manifest>
